<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>fun.commons</groupId>
        <artifactId>token-gateway</artifactId>
        <version>0.3.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!-- JaCoCo 门禁: 实测 78.8% (2026-09-02 提升后) -->
    <properties>
        <jacoco.line.min>0.75</jacoco.line.min>
    </properties>

    <artifactId>face-llm</artifactId>
    <name>face-llm</name>
    <description>
        token-gateway LLM 同步面 (face=llm 部署分组, 无数据库无本地盘, 弹性扩缩):
        6 端点 controller + RelayOrchestrator 管线 (validate→resolve→审核→preConsume→saga) +
        SSE 透传 upstream + OpenAI/Anthropic 协议转换 format.
    </description>

    <dependencies>
        <dependency>
            <groupId>fun.commons</groupId>
            <artifactId>gateway-core</artifactId>
        </dependency>

        <!-- fastjson2 (FormatConverter / SseConverter 解析上游 chunk JSON) -->
        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
            <version>2.0.41</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <version>4.12.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
